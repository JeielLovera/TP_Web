@model IEnumerable<Entity.Rol>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CRol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NRol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MSueldo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CRol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NRol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MSueldo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
