@model IEnumerable<Entity.Venta_Producto>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CVenta_Producto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CProducto.NProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CVenta.CVenta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QCantidad)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CVenta_Producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CProducto.NProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CVenta.CVenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QCantidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CVenta_Producto  }) |
                @Html.ActionLink("Details", "Details", new { id=item.CVenta_Producto }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CVenta_Producto })
            </td>
        </tr>
    }

</table>
